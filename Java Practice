
// Can you find the bug?
public class BeansInJars {
   public static void main (String [] args) {
      int numBeans;
      int numJars;
      int totalBeans;

      numBeans = 500;
      numJars = 3;
   
      System.out.print(numBeans + " beans in ");
      System.out.print(numJars + " jars yields "); 
      totalBeans = numBeans * numJars; 
      System.out.println(totalBeans + " total");  
   }
}


import java.util.Scanner;

public class IntegerExpressions {
	public static void main(String[] args) {
		Scanner scannedInt = new Scanner(System.in);
      int firstInt;
      int secondInt;
      int thirdInt;
		
	   firstInt = scannedInt.nextInt();
	   secondInt = scannedInt.nextInt();
	   thirdInt = scannedInt.nextInt();

